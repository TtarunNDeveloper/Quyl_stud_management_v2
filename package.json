{
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "date-fns": "^4.1.0"
  },
  "prisma": {
    "schema": "backend/prisma/schema.prisma"
  },
  
    "scripts": {
      "start": "webpack --openssl-legacy-provider",
      "build": "webpack --openssl-legacy-provider"
    }
}

